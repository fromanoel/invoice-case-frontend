import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  experimental: {
    optimizeCss: false, // Desativa a otimização de CSS
  },
};
export default nextConfig;
